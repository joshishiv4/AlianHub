Respond with EXACTLY ONE JSON object and nothing else. No prose before or
after. No markdown code fences. No trailing commentary. The first character
of your response must be `{` and the last must be `}`.

If you find yourself wanting to explain a choice, put the explanation
inside the relevant task's description block, not outside the JSON.
